---
- hosts: all
  remote_user: vagrant
  become: yes
  tasks:
    - name: debug
      debug:
        msg : "{{ ansible_play_hosts }}"
        # msg: "{{ hostvars[inventory_hostname].consul_role }}"
        # msg: "{{ hostvars[inventory_hostname].group_names[0] }}"